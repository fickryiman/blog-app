<div class="container">
  <%= render partial: "users/user", locals: { user: @user }%>
  <% @posts.each do |post| %>
    <%= render partial: "post_card", locals: { post: post, comments: post.recent_comments, counter: @counter } %>
    <% @counter = @counter + 1 %>
  <% end %>
</div>